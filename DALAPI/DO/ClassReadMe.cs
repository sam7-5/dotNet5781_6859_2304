namespace DAL.DO
{
    class ClassReadMe
    {
        /*-chaque class doit etre dans un fichier differents sous namespace BO
        -public
        - pds( passive data structure)= uniquement des meafyenime(de type valuetype uniquement) pas de code ni de ctor a part to string
        -pas de list ni de ossef
        -pas de inheritance
        -objet de type valuetype pas ref
        -doit contenir toute les donnees necessaire a BL
       */
    }
}
