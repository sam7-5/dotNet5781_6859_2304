<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
         Title="go c# go!" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen" Opacity="0.99" >
    <Window.Resources>
        <Color x:Key="Color1">#FFB4B4B4</Color>
    </Window.Resources>

    <Grid Opacity="0.8">

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.Background>
                <ImageBrush ImageSource="Resources/bus1.jpg"/>
            </Grid.Background>
        </Grid>
        <Button Name="admin" Click="admin_Click"  Foreground="White" BorderBrush="White" Content="Administration" Padding="10" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="40" Width="150" Height="40" Background="Transparent"/>
    </Grid>
</Window>
