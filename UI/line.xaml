<Page x:Class="UI.line"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UI"
        xmlns:BO="clr-namespace:BO;assembly=Bl"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="line">

    <Page.Resources>
        <CollectionViewSource x:Key="guestRequestViewSource"/>
    </Page.Resources>
    <Page.Background>
        <ImageBrush ImageSource="bus1.jpg" Opacity="0.5"/>
    </Page.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Name="update_line" Content="update current line" Grid.Column="2" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="10" Grid.Row="1" Margin="27.4,10,0,10.2" Width="211" Height="23" Grid.RowSpan="2" Background="#FFD9C51B" />
            <Button Name="add_line" Content="add line" HorizontalAlignment="Left" Margin="31,10,0,0" Grid.Row="1" VerticalAlignment="Top" Width="212" Height="23" Grid.RowSpan="2" Background="#FF49D44B"/>
            <Button Name="delete_line" Content="delete current line" Grid.Column="1" HorizontalAlignment="Left" Margin="24.8,10,0,0" Grid.Row="1" VerticalAlignment="Top" Width="212" Height="23" Grid.RowSpan="2" RenderTransformOrigin="0.365,1.225" Background="#FFDC2E28"/>
        </Grid>

        <DataGrid x:Name="ds_guestRequestDataGrid" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Visible"
                  AlternatingRowBackground="AliceBlue" RowDetailsVisibilityMode="VisibleWhenSelected" ItemsSource="{Binding }" EnableRowVirtualization="True" Background="Transparent" AutoGenerateColumns="False" IsReadOnly="True" Height="auto" Padding="10" Margin="0,43,608,-0.4" Grid.RowSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="nameColumn" MinWidth="100" Width="*" Header="Name" Binding="{Binding Name}"  IsReadOnly="True" />
                <DataGridTextColumn x:Name="lastNameColumn" MinWidth="100" Width="*" Header="Last Name" Binding="{Binding LastName}" IsReadOnly="True"/>
                <DataGridTextColumn x:Name="dg_Email" Width="*" MinWidth="100" Header="Email" Binding="{Binding Mail.Address}" IsReadOnly="True" />
                <DataGridTextColumn x:Name="guestRequestKeyColumn" MinWidth="100" Width="*" Header="Request Key" Binding="{Binding GuestRequestKey}" IsReadOnly="True"/>
                <DataGridTemplateColumn x:Name="dg_registrationColumn" Width="*" Header="Registration Date" MinWidth="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate x:Name="dt_registrationDate">
                            <DatePicker x:Name="dp_registrationDate" SelectedDate="{Binding Registration}" SelectedDateFormat="Short" Focusable="false" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn x:Name="statusColumn" Width="*" MinWidth="100" Header="Order Status" Binding="{Binding Status}" IsReadOnly="True"/>

            </DataGrid.Columns>
        </DataGrid>
        <DataGrid HorizontalAlignment="Left" Height="387" Margin="216,9.8,0,0" Grid.Row="1" VerticalAlignment="Top" Width="556" Background="Transparent"/>
    </Grid>
</Page>
